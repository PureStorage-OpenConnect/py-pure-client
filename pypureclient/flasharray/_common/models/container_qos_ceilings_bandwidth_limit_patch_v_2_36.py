# coding: utf-8

"""
    FlashArray REST API

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 2.36
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
from inspect import getfullargspec
import json
import pprint
import re  # noqa: F401

from typing import Any, List, Optional
from typing import Union, Any, List, TYPE_CHECKING
try:
    from pydantic.v1 import BaseModel, Field, StrictStr, ValidationError, conint, constr, validator
    from pydantic.v1 import StrictStr, Field
except ModuleNotFoundError:
    from pydantic import BaseModel, Field, StrictStr, ValidationError, conint, constr, validator
    from pydantic import StrictStr, Field

CONTAINERQOSCEILINGSBANDWIDTHLIMITPATCH_ONE_OF_SCHEMAS = ["int", "str"]

class ContainerQosCeilingsBandwidthLimitPatch(BaseModel):
    """
    The maximum QoS bandwidth limit for the container. Whenever throughput exceeds the bandwidth limit, throttling occurs. Measured in bytes per second. Maximum limit is 512 GB/s. In order to unset use \"\".
    """
    # data type: int
    oneof_schema_1_validator: Optional[conint(strict=True, le=549755813888, ge=1048576)] = None
    # data type: str
    oneof_schema_2_validator: Optional[constr(strict=True, max_length=0, min_length=0)] = None
    if TYPE_CHECKING:
        actual_instance: Union[int, str]
    else:
        actual_instance: Any
    one_of_schemas: List[str] = Field(CONTAINERQOSCEILINGSBANDWIDTHLIMITPATCH_ONE_OF_SCHEMAS, const=True)

    class Config:
        validate_assignment = True

    @classmethod
    def validate(cls, value):
        """Backwards compatibility: accept a raw value and wrap it as actual_instance.

        Pydantic calls this hook whenever ContainerQosCeilingsBandwidthLimitPatch is used as a field type
        on another model (and again on assignment, via Config.validate_assignment).
        Older client code assigned the raw oneOf member value instead of the
        wrapper, e.g.:

            model.field = "raw-string-value"        # raw primitive member
            model.field = MemberModel(name="x")     # raw member model instance

        instead of the strict form:

            model.field = ContainerQosCeilingsBandwidthLimitPatch("raw-string-value")

        This hook coerces all of those into a ContainerQosCeilingsBandwidthLimitPatch so the
        `actual_instance` oneOf validator still runs against the wrapped value.
        """
        # Already a ContainerQosCeilingsBandwidthLimitPatch: pass through untouched.
        if isinstance(value, cls):
            return value
        # A different oneOf wrapper holding a compatible payload — a sibling
        # wrapper class, or the same wrapper from another API-version module.
        # Stock pydantic v1 accepts these via its `cls(**dict(value))` fallback,
        # so clients released without this hook allow them; unwrap and re-wrap
        # to preserve that (the payload still runs the oneOf validator).
        # NOTE: this is only safe while coerce targets have primitive-only
        # oneOf members; a member that is itself a wrapper model would be
        # incorrectly unwrapped here.
        if isinstance(value, BaseModel) and hasattr(value, "actual_instance"):
            return cls(value.actual_instance)
        # A serialized wrapper dict (wrapper.dict() -> {"actual_instance": ...}):
        # rebuild the wrapper from its fields. Any other dict falls through to
        # cls(value) and fails the oneOf validation loudly — stock pydantic
        # would instead build a wrapper with actual_instance=None and silently
        # serialize the field as null.
        if isinstance(value, dict) and "actual_instance" in value:
            return cls(**value)
        # Anything else is a raw member value (str, int, member model instance, ...):
        # wrap it so the oneOf validator can match it against the allowed schemas.
        return cls(value)


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = ContainerQosCeilingsBandwidthLimitPatch.construct()
        error_messages = []
        match = 0
        # validate data type: int
        try:
            instance.oneof_schema_1_validator = v
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # validate data type: str
        try:
            instance.oneof_schema_2_validator = v
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in ContainerQosCeilingsBandwidthLimitPatch with oneOf schemas: int, str. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in ContainerQosCeilingsBandwidthLimitPatch with oneOf schemas: int, str. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: dict) -> ContainerQosCeilingsBandwidthLimitPatch:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> ContainerQosCeilingsBandwidthLimitPatch:
        """Returns the object represented by the json string"""
        return ContainerQosCeilingsBandwidthLimitPatch.construct(_fields_set=None, **{
            "actual_instance": json.loads(json_str)
        })

    def as_request_dict(self) -> Dict[str, Any]:
        return self.to_dict(include_readonly=False)

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        to_json = getattr(self.actual_instance, "to_json", None)
        if callable(to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self, include_readonly: bool = True) -> dict:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        to_dict = getattr(self.actual_instance, "to_dict", None)
        if callable(to_dict):
            return self.actual_instance.to_dict(include_readonly=include_readonly)
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.dict())


